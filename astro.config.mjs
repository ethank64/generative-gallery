import { defineConfig } from 'astro/config';

// Project page on GitHub Pages → served from /generative-gallery/
export default defineConfig({
  site: 'https://ethank64.github.io',
  base: '/generative-gallery',
});
